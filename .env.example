# Copy to .env and fill values. Do NOT commit real keys.
# For Google Vertex AI / Gemini you typically need either Application Default Credentials
# (set GOOGLE_APPLICATION_CREDENTIALS to a service-account json) or a valid API key.
# Example env vars used by this app:
GCP_PROJECT=your-gcp-project-id
GCP_LOCATION=us-central1
# Optional: API key (may not be sufficient for Vertex AI in some configurations)
GOOGLE_API_KEY=
# If using Vertex AI via Google Cloud SDK, set path to service account JSON:
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
# Optional: choose model name
GEMINI_MODEL=gemini-pro
PORT=8080